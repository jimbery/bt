#!/bin/sh
# Run from repo root after: git config core.hooksPath .githooks
# Runs: make precommit → lint, race tests, then orders-api + bt (table, property, fuzz).
set -e
cd "$(git rev-parse --show-toplevel)"
make precommit
